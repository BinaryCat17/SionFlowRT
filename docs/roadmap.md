# SionFlowRT Roadmap: Pure Dataflow Orchestration 🌀

## Vision: "Graph is the Truth"
SionFlowRT — это среда для генерации производительного C-кода из графов вычислений. Система статически анализирует топологию связей (`links`) и ресурсы (`sources`), чтобы автоматически управлять памятью, состояниями и параллельным исполнением.

---

## 1. Global Link Analysis (Линковка и Состояния) 🕸️
- [ ] **Unified Connection Graph**: Построение глобального графа связей между модулями и `sources`.
- [ ] **Automatic State Detection**: 
    - Любой цикл в графе линков (`A.out -> ... -> A.in`) автоматически распознается как **State**.
    - Реализация механизма **Double Buffering** (Front/Back) для таких связей (переключение указателей).
- [ ] **Topological Shape Propagation**: 
    - Формы «текут» от `sources` и констант через весь глобальный граф.
    - Ошибка компиляции, если в графе остались неразрешенные (`_`) или неопределенные размерности.

## 2. Multi-Actor Execution (Параллелизм) 🎭
- [ ] **Threaded Execution**: Каждая программа из манифеста генерируется как независимая функция, исполняемая в своем потоке.
- [ ] **Zero-Copy Synchronization**: 
    - Использование Triple Buffering для линков между программами, работающими с разной частотой.
    - Прямая передача указателей на буферы вместо копирования данных через `memcpy`.
- [ ] **Lock-free Barriers**: Генерация минимальных атомарных инструкций синхронизации для обмена данными между потоками.

## 3. Advanced Memory & Indexing Engine 🔀
- [ ] **Stride-based Memory Model**: 
    - Переход от плоского `buffer[i]` к индексации через страйды: `index = sum(coord[i] * stride[i])`.
    - Это позволяет делать `Transpose` и `Slice` без операций над памятью, просто меняя метаданные.
- [ ] **Memory Coloring**: Статический анализ времени жизни ресурсов для переиспользования буферов и минимизации потребления RAM.
- [ ] **Broadcasting**: Автоматическая поддержка операций над тензорами разной размерности (Broadcasting) на уровне генерации циклов.
