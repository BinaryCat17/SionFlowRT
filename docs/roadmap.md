# SionFlowRT Roadmap: The Symbolic Era 🌀

## Vision
SionFlowRT — это не просто компилятор графов, это **Symbolic Metadata Engine**. Мы разделяем топологию вычислений (что делать) и метаданные (над какими формами и типами). Это позволяет создавать универсальные математические ядра, которые адаптируются под любые данные без переписывания JSON.

---

## 1. Symbolic Shape Engine 🧠
Внедрение полноценного символьного ядра для управления размерностями.
- [x] **Refactored Dimension Enum**: Переход от строк к типам `Value`, `Symbol`, `Wildcard`, `Ellipsis`.
- [x] **Shape Solver**: Автоматический вывод `_` (Wildcard) в Reshape на основе сохранения объема.
- [x] **Dynamic Ellipsis**: Расширение эллипсисов `...` при стыковке портов и в операциях.
- [x] **Symbolic Arithmetic**: Упрощение выражений в формах (напр. `N * 1 -> N`, `N + 0 -> N`).
- [ ] **Complex Expressions**: Поддержка выражений вида `(N*M)/2` в JSON (Symbolic Parser).

## 2. Generic Type System 🎭
Абстракция данных от конкретных типов процессора.
- [x] **Abstract Types**: В JSON используются только `float` и `int`.
- [x] **Manifest Mapping**: Конкретизация типов (F32, F64, I32) на этапе генерации кода через конфиг.
- [x] **Unified Namespace**: Внедрение префиксов `inputs.`, `outputs.`, `sources.` для строгой типизации связей.
- [ ] **Type Propagation**: Автоматический вывод типов для промежуточных узлов (напр. `float * int -> float`).

## 3. Manifest-Driven Architecture 🗺️
Манифест как описание топологии верхнего уровня.
- [x] **Topological Links**: Связи между программами и источниками описываются как граф линков.
- [x] **Context Injection**: Автоматический проброс параметров окна (`WIDTH`, `HEIGHT`) в символьный движок.
- [x] **Zero-Magic**: Устранение хардкода системных источников в коде компилятора.

## 4. Virtual Layout & Strides 🔀
Максимальная производительность через Zero-copy операции.
- [ ] **Stride-based Indexing**: Каждая операция работает не с плоским индексом `i`, а с формулой `sum(coord[d] * stride[d])`.
- [ ] **Metadata-only Transpose**: Транспонирование через изменение страйдов, без физического перемещения данных.
- [ ] **Symbolic Index Codegen**: Генерация оптимизированных формул доступа к памяти в C-коде.

## 5. Optimization & Codegen ⚡
- [ ] **Loop Fusion**: Объединение цепочек операций в единые `for`-циклы на основе общих символьных границ.
- [ ] **Static Memory Planner**: Расчет пикового потребления памяти на основе символьных выражений.
- [ ] **Parallel C Backend**: Использование OpenMP для распараллеливания по внешним символьным размерностям.

---

## Итоги спринта "Архитектурная чистота" 🎯
1. Создан `ShapeEngine.rs` — сердце символьных вычислений.
2. Библиотека `math/` очищена от legacy и переведена на новый стандарт.
3. Примитив `Clamp` успешно заменен на библиотечный сабграф (Minimal Kernel approach).
4. Внедрена строгая система имен портов с префиксами.
